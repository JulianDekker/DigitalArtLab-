{% extends '::base.html.twig' %}

{% block body -%}

    {% if user.aanwezig == 1 %}
        <h1>Welkom {{ user.firstname }} {{ user.lastname }}</h1>
    {% else %}
        <h1>Succesvol Afgemeld</h1>
    {% endif %}

{% endblock %}
